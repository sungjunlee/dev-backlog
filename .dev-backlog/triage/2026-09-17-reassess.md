# Reassess Report — 2026-09-17

Report-only stale-spec review triggered by the sprint-close reassess signal
(doctor clean; 6 sprints closed since `2026-07-27-reassess.md`, threshold 3).
First reassess under the status-free charter (rev 15+): there is no status
ladder to re-evaluate, so the report checks each living Objective and
capability against what shipped, names contradictions, and proposes
amendments for one human gate. No spec file is edited by this report.

Issue: #421 (epic #420). Sprint: `.dev-backlog/sprints/2026-09-subtraction-wave.md`.

## Sprints covered

| Sprint | Closed | Shipped |
| --- | --- | --- |
| `2026-07-github-native-core-simplification` | 2026-07 | mirrorless GitHub core (#351–#354) |
| `2026-07-progress-axis-removal` | 2026-07 | progress-sync / sprint-mirror axis deleted (#340) |
| `2026-07-rule-simplification` | 2026-07 | scopeless-track warning rule (#337) |
| `2026-08-review-follow-ups` | 2026-08 | review pins, spec-history fold (#386–#388) |
| `2026-08-second-start-lightening` | 2026-08 | status-free charter, personal-toolkit posture (#369–#382) |
| `2026-09-skill-lightening` | 2026-09-12 | SKILL.md 225 → 159, evals to `tests/evals/`, conformance run (#396–#401, #408) |

The #411 tracker wave (#412–#415, merged 2026-09-15) landed after the last
close and is the main source of drift below.

## Evidence

- `backlog-doctor --json`: all checks pass; `reassess_signal.fired: true`,
  `sprints_since_last_report: 6`.
- Charter is at revision 17 (last amended 2026-08-21). Capabilities and
  system map were amended by #413–#415 on 2026-09-15; the charter was not.
- `TRACKER_KEYS` in `scripts/tracker.js` is `["github", "files", "gitlab"]`.
- Measured consumers on the maintainer machine: every `.dev-backlog/.tracker`
  and legacy `backlog/.tracker` checkout pins `github`; none pins `files` or
  `gitlab`. The Backlog.md CLI (`backlog` 1.50.1) is installed; `glab` is not.
- Spec-axis linters (`objectives-check.js`, `component-lint.js`,
  `capabilities-doctor.js`, `spec-paths.js`) are read only by
  `backlog-doctor.js` and `sprint-init.js`; no consumer repo, relay, or triage
  path consumes their JSON. Their last non-pass verdict in this repo predates
  rev 15.

## Contradictions

1. **Charter vs code on tracker adapters.** `charter.md` Non-Goals line 28:
   "GitHub Issues own task truth; no new tracker adapters". `capabilities.md`
   `tracker-task-truth` In-scope lists `files` and `gitlab`; `system-map.md`
   Runtime Boundaries names all three. Code ships three adapters. The
   2026-08-17 freeze Decision ("no new features ... without a measured
   consumer") was applied to `files` (Backlog.md CLI installed and in use) but
   not to `gitlab` (no CLI, no pinning repo).
2. **Charter Approach and O10 still say "GitHub Issues" where the shipped
   rule is "the configured tracker".** Capabilities already use "configured
   tracker" wording; the charter does not.
3. **`capabilities.md` header says `component:` "names exactly one slug"
   and "Completed sprints that name a retired slug are not re-linted"** —
   both sentences exist only because `component-lint` exists. The
   `scopesOverlap` predicate (the only runtime consumer of `component:`)
   compares strings; it never resolves a heading.
4. **`_context.md` gotcha** "#366 and #367 stay parked until organic pain
   appears": #366 closed 2026-08-22; #367's cadence ran on 2026-09-12 and is
   the basis of #423. Corrected in this PR (not spec).

## Proposed amendments (human-gated; approved and applied 2026-09-17)

### `spec/charter.md` → revision 18

- **Approach**, first sentence: "Use GitHub Issues as the sole
  task-definition and lifecycle authority." → "Use one configured tracker
  (GitHub Issues by default) as the sole task-definition and lifecycle
  authority."
- **Non-Goals**, Tracker generality bullet, replace with:
  "Tracker generality — one configured tracker per repo owns task truth.
  Adapters are thin CLI translations on the frozen ports
  (`references/adapter-ports.md`); a new adapter is admitted only for a
  **measured consumer**: the tracker's CLI is installed and in use on a
  maintainer machine, or a maintainer-run repo already pins the key. No
  multi-master sync, no SaaS connectors (Jira, Linear, Notion). Backlog.md beyond the `files` adapter survives only as explicit
  one-way diagnostic export."
- **O10**: "GitHub Issues are the standalone task-definition and lifecycle
  authority" → "The configured tracker (GitHub Issues by default) is the
  standalone task-definition and lifecycle authority"; rest unchanged.
- **Decisions**, append two rows dated on the gate date:
  1. "Tracker rule as shipped: one configured authority per repo; adapters
     are CLI translation on frozen ports; a new adapter requires a measured
     consumer. `files` qualifies (Backlog.md CLI installed and in use);
     `gitlab` does not (no `glab`, no pinning repo) and is parked at its
     merge commit (#421, #424)" |
     rationale: the 2026-08-17 freeze already said seams expand only for a
     measured consumer; #415 shipped without one | supersedes: rev-17 "no new
     tracker adapters" non-goal and the implicit three-key freeze.
  2. "Spec-axis linters are removed; `objectives:` and `component:` are
     unchecked sprint metadata. At runtime `component:` is a free
     track-scope string compared only by `scopesOverlap`; naming a
     capability heading stays a routing convention for relay Learnings, not
     a checked contract. O4 (open-issue drift) is served by the
     `backlog-triage` Alignment section, which is the automated detection
     path; frontmatter linting checked sprint metadata, never open-issue
     drift, so O4 is unchanged (#421, #426)" | rationale: the linters were
     the same species of upkeep as the rev-15 status ladder, and no consumer
     reads their output | supersedes: rev-14 "component resolves to a
     capability heading" contract.

### `spec/capabilities.md`

- Header (lines 5–12): "Sprint `component:` names exactly one slug" →
  "Sprint `component:` is a free track-scope string; by convention it names
  a capability heading here so relay Learnings can route, but nothing lints
  it." Delete "Completed sprints that name a retired slug are not re-linted."
- `tracker-task-truth`: remove the `.tracker=gitlab` In-scope bullet; replace
  the Out-of-scope "New tracker adapters beyond ... `gitlab` keys" bullet with
  "New tracker adapters without a measured consumer (charter rule); GitLab,
  Forgejo, and Gitea remain follow-ups"; Expected Behaviors identity list
  becomes "(`#N` or `BACK-N`)"; Hard Constraint "Never fall back from
  `github` to `files` or `gitlab`" → "Never fall back between configured
  trackers". Append a Decisions row for the parking of `gitlab` (#424)
  citing the merge commit for retrieval.
- `sprint-execution`: append a Decisions row for the linter removal (#426);
  In-scope line "the track-scope key: `component:` or `scope:`" stays.

### `spec/system-map.md`

- Runtime Boundaries: drop "When `.tracker=gitlab`, GitLab Issues via `glab`
  own that role."
- Storage: `.dev-backlog/.tracker` — "`github` or `files`"; drop the
  `gitlab` line; keep "Forgejo/Gitea are follow-up forge adapters" as
  "GitLab/Forgejo/Gitea are follow-up forge adapters (measured consumer
  required)".
- Invariants: "GitHub unavailability never selects files, GitLab, or any
  other store" → "never selects another tracker or any local store".

## No Change

- **O1** (shared sprint file per admitted track) and **O4** (drift
  detectable without manual triage) are unaffected by the tracker wave.
- **`sprint-execution`** Expected Behaviors and Hard Constraints stay;
  overlap refusal, `[~]` pointer rule, and immutable `status: completed`
  all pass the keep test (shared / irreversible state).
- **`triage-grooming`** unchanged; it does not read the linters.

## Execution after the gate

The spec pass alone does not finish either change; the gated issues carry
the execution-contract and reference edits:

- **#424** also removes `gitlab` from `references/adapter-ports.md` (the
  three-key `TRACKER_KEYS` freeze line, the per-adapter surface table, and
  the GitLab implementation paragraph) and from SKILL.md frontmatter if any
  mention remains.
- **#426** also rewrites the SKILL.md Sprint File Contract `component:` row
  ("One capability slug ... resolves to one `## Capability:` heading") to
  the free-string wording, and trims `references/spec-fallback.md`.

## Verdict

Axis is stale on one rule (tracker adapters) and one implementation-shaped
sentence (`component:` resolution). The spec side is fixed by a single
human-gated pass covering charter rev 18, capabilities, and system map;
#424 and #426 then apply the matching contract and reference edits listed
above. This dated report resets the reassess counter.
