# Reassess Report — 2026-07-07

Report-only stale-spec review triggered by the sprint-close reassess signal (doctor clean; 3 sprints closed since the last reassess `2026-07-04`, threshold 3). Report-only: no spec files were edited. Accepted fixes route through `spec-charter amend`, `spec-system-map amend`, `spec-grill <capability>`, or a user-approved Learning Action.

## Evidence

- `capabilities-doctor --json`: `ok`, 4 capabilities, 154 lines, no warnings/issues — capability spec is healthy and within budget.
- `component-lint --json`: no issues; 11 routed / 7 unrouted sprints (unrouted are pre-spec-axis legacy sprints — expected, not drift).
- `backlog-doctor` at close: all 8 checks PASS.
- 2026-07 adoption-hardening sprint (10 issues, just closed) hardened one cross-cutting theme: a repo with **no craftkit and no `spec/`** can complete a full sprint cycle from this bundle alone (new `references/spec-fallback.md`, `sprint-init` field omission, zero `../spec-charter/` reads in `skills/`, minimum-path README).
- `spec/capabilities.md` sprint-execution block: Expected Behaviors do **not** mention that `objectives:`/`component:` are optional and omitted when the spec file is absent — behavior shipped in #258 but not in the contract.
- `spec/system-map.md:30` frames the craftkit spec-* skills as "installed as sibling skills"; E2 removed the required sibling-path *consumption* dependency (now in-bundle `spec-fallback.md`, craftkit as when-installed enhancement).

## No Change

- Charter Objectives O1–O5 still match evidence (all `[validated]`/`[active]` with proof); O6 correctly `[deferred]`.
- `backlog-sync`, `triage-grooming`, `task-progress-reporting` capability contracts — no new durable behavior or repeated worked-around constraint this cycle.
- system-map core flows and invariants (authoring gates still owned by craftkit; report-only automation toward `spec/*`) remain accurate.

## System Map Candidates

- `spec/system-map.md:30` "installed as sibling skills" — evidence: E2 (#254/#255) removed the required `../spec-charter/` consumption path; the in-bundle `references/spec-fallback.md` now carries consumption-side degradation and craftkit is a when-installed enhancement. Suspected change: clarify that dev-backlog **consumes** spec output with a local fallback and does not require the sibling skills at runtime. Weak candidate — the authoring-gate statement itself is still true. next: `spec-system-map amend` (optional).

## Grill Candidates

- `sprint-execution` — evidence: #258 shipped optional spec-frontmatter (omit `objectives:`/`component:` when the backing spec file is absent; doctor soft-warns only on active-sprint omission with spec present), but the capability's Expected Behaviors don't state it. Suspected change: add one Behavior line for optional-and-omitted spec fields. next: `spec-grill sprint-execution`.

## Amend Candidates

- **Cold-adopter portability objective** — evidence: an entire 10-issue sprint plus PRD §8 (`docs/prd-2026-07-adoption-hardening.md`) hardened "works standalone without craftkit/spec," a theme that spans sprint-execution, backlog-sync, and triage-grooming. No current Objective captures it (O1/O3 are about shared execution state and the reference axis, not bundle-standalone reach). Suspected change: add an Objective such as *"A repo with no craftkit and no `spec/` files can complete a full sprint cycle from this bundle alone, with no dangling cross-repo pointers."* next: `spec-charter amend` (human-gated).

## Learning Actions

- Keep inline: sprint-execution's recent 2026-07 Learnings (relay-run substrate lessons, position-independent bash flag parsing) — recent, high-signal, still startup context.
- Promote: none — no Learning has repeated across capabilities enough to become a Decision this cycle.
- Archive: none — no history has aged out of the hot startup path.

## Missing Evidence

- None skipped; all optional scripts ran and all three `spec/*` files were present.

## Recommended Next Step

- `spec-charter amend` to add the cold-adopter portability Objective — it is the strongest, most cross-cutting signal and is the PRD §8 candidate that this sprint deliberately did **not** self-approve. Human-gated. (The `spec-grill sprint-execution` and optional `spec-system-map amend` candidates can follow or be deferred; none is urgent — the specs are healthy today.)
