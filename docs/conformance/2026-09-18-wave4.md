# Conformance: 2026-09 subtraction wave 4 (epic #467)

After-run only. The wave touched no SKILL.md rail (157 lines, byte-identical to v0.13.0) and no eval scenario, so the prompt is identical to the wave-3 run (`prompt-wave4.md` diffs empty against the pruned `2026-09-18-w3/prompt-wave3.md`) and one after-run per model is the before/after pair. Harness as in `2026-09-17-skill-lightening.md`: Expected stripped, eleven scenarios, rubric PASS/PARTIAL/FAIL with ASK/MUT flags.

## What changed under the models (v0.13.0 → main)

| Surface | v0.13.0 | main |
| --- | ---: | ---: |
| `next.sh` | 157 | 34 (argument plumbing; `exec sprint-state.js --mode next --format text`) |
| `status.sh` | 109 | 38 (same; `--mode status`) |
| `lib.sh` | 111 | 105 (`next_todo_item` gone; the rest still serve `sprint-close.sh` and the smoke test) |
| `sprint-state.js` | 504 | 693 (`--format text` renderer; JSON schema v2 byte-identical) |
| `skills/dev-backlog/scripts/` | 2,255 lines / 11 files | 2,244 / 11 |
| `tests/smoke/smoke-test.sh` | 1,240 | 1,199 (`GATE_*` / `gated_assert` gone; 163/163 assertions unchanged) |
| `tests/` total | 4,653 | 4,673 (12 node cases for the text renderer and CLI exit codes) |

The line count barely moves; the point is one parser for the human text surface and the dev-relay JSON surface. Text mode now follows the JSON semantics (Plan-only counts, `started`-ordered portfolio, fail-loud overlap, authority-neutral no-sprint hint).

## Runs

| Channel × variant | PASS | PARTIAL | FAIL | ASK | MUT | Notes |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| fable × wave4 | 11/11 | 0 | 0 | 0 | 0 | 8 fail-closed on a failed `gh` read; 11 names the setup migration and asks before moving files (Expected allows either); 2 refuses via `sprint-init.js` and offers the three resolutions |
| astra × wave4 | 11/11 | 0 | 0 | 0 | 1 | Plan fold offered in 2 (every Astra run since the first wave); 8 stops; 11 runs the documented migration and orients on `--json` rails |

Identical profile to the wave-3 after-run (fable 11/11 · 0/0, astra 11/11 · 0/1). Every run that named `next.sh` / `status.sh` used them as the orient rail with `--json` or `--track`; no run described bash-side parsing, `next_todo_item`, a `gh issue list` hint, or any smoke gate.

Raw: `2026-09-18-w4/fable-wave4.md`, `astra-wave4.md`, `prompt-wave4.md`. The wave-3 raw directory is pruned in this commit per the retention rule; `2026-09-18-wave3.md` stays as the record.

## Reviews

- #471 (session, #469): Sol via codex, MERGE.
- #473 (Opus subagent, #468): Astra via codex, DO NOT MERGE — the wrappers stopped sourcing `lib.sh` and so began honouring a `DEFAULT_BACKLOG_DIR` environment variable the old scripts always overwrote; the CHANGELOG claimed text was byte-identical when edge cases (Plan-only counts, `started` order, blank-line trimming, overlap exit 1) differ; no CLI-level exit-code test. All three applied as a direct fix; the semantic changes are declared, not reverted, because they are the JSON semantics the issue set out to make singular. Merged after the fix.
- Release PR: Astra via codex, cumulative v0.13.0..main; verdict recorded on #470.
