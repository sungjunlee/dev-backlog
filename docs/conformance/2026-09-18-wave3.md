# Conformance: 2026-09 subtraction wave 3 (epic #456)

After-run only. The wave touched no SKILL.md rail (157 lines / never-only-must 5, byte-identical to v0.12.0) and no eval scenario, so the prompt is identical to the v0.12.0 baseline run (`2026-09-17-cc/`, after #446) and one after-run per model is the before/after pair. Harness as in `2026-09-17-skill-lightening.md`: Expected stripped, eleven scenarios, rubric PASS/PARTIAL/FAIL with ASK/MUT flags.

## What changed under the models (v0.12.0 → main)

| Surface | v0.12.0 | main |
| --- | ---: | ---: |
| `skills/dev-backlog/scripts/` | 2,807 lines / 13 files | 2,255 lines / 11 files |
| `sprint-init.js` | 372 | 243 (`--dry-run`, `--json`, structured refusal, `issueCount`/`placeholderIssue` gone; unknown flags and a valueless `--milestone` now rejected before any effect) |
| `backlog-doctor.js` | 490 | 389 (`in_flight_staleness`, `context_bloat`, `--stale-days` gone) |
| `lib.js` | 308 | 239 (`parseMarkerMonth`, triage config readers gone) |
| `status.sh` | 163 | 109 (Local Files, Relay Runs, Past sprints gone) |
| moved to `tests/tools/` | — | `doc-drift-check.js`, `bash-runtime.js` |
| `tests/` | 4,623 | 4,653 (the two moved helpers add 199; deletions remove 169) |

The v0.12.0 file count was reported as 14 in the epic #440 close-out; the actual count was 13.

## Runs

| Channel × variant | PASS | PARTIAL | FAIL | ASK | MUT | Notes |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| fable × wave3 | 11/11 | 0 | 0 | 0 | 0 | 8 fail-closed on a failed `gh` read; 11 names the setup migration and asks before moving files (Expected allows either) |
| astra × wave3 | 11/11 | 0 | 0 | 0 | 1 | Plan fold offered in 2 (every Astra run since Batch 1); 11 orients read-only and names the migration on the Plan rail |

Identical profile to the v0.12.0 baseline (fable 11/11 · 0/0, astra 11/11 · 0/1). No run named `--dry-run`, `--stale-days`, a staleness or bloat verdict, a Local Files or Relay Runs block, or either moved helper; every run that named `sprint-init.js` gave it only `--component`/`--scope`, and every run that named the doctor described it as the pre-close verdict printer.

Raw: `2026-09-18-w3/fable-wave3.md`, `astra-wave3.md`, `prompt-wave3.md`.

## Reviews

- #462 (session): Sol via codex, MERGE WITH EDITS — keep the helper tests under `tests/dev-backlog/`; applied.
- #463 (session): Sol via codex, MERGE WITH EDITS — 109 lines vs a ≤ 100 estimate; acceptance amended to ≤ 110 (remaining lines are the protected Active Sprint block; cut, do not refactor).
- #464 (Grok 4.6): Astra via codex, DO NOT MERGE — retired flags were silently accepted, turning a former dry-run into a write; fixed by rejecting unknown flags and a valueless `--milestone`, plus four restored assertions. Merged after the fix.
- #465 (Grok 4.6): Astra via codex, MERGE.
- Cumulative v0.12.0..main: Astra via codex; verdict recorded on #461.
