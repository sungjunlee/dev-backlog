# Conformance: v0.16.0 relay deprecation (PRs #481–#487)

Before/after pair. The wave is mostly deletion and wording — the relay sweep, the `related-skills` prune, the docs/dogfood prune — but #486 changed the Complete rail's close condition in SKILL.md, and `README.md` asks for a before/after pair on any rail change. **Before** = the current thirteen-scenario `tests/evals/dev-backlog.md` × the v0.15.0 SKILL.md (`prompt-v0160before.md`); **after** = the same eval file × the release SKILL.md (`prompt-v0160.md`). The only eval text change since wave 5 is scenario 5 dropping "and no Relay". Harness as described in `README.md`, graded by the session against the `Expected:` text in `tests/evals/dev-backlog.md`; this run grades strictly (an Expected clause the answer does not state is a PARTIAL), which is stricter than wave 5 graded the same answers — see the note under the table.

## What changed under the models (v0.15.0 → main)

| Surface | v0.15.0 | main |
| --- | --- | --- |
| Delegate | dev-relay named in SKILL.md, references, README, and `spec/*`; `[run:id]` Plan pointer; `sprint-state` JSON `run_id` field | no delegate named; a legacy trailing `[run:…]` is stripped and ignored (the PR pointer and title survive); `run_id` deleted; the JSON is documented as the fresh-session recovery rail |
| `SKILL.md` | 165 lines; Complete rail said the sprint stays open until its Plan is done | 164 lines; the rail states the close condition — every Plan item `[x]`, or the rest struck or carried with a Progress entry |
| `related-skills` frontmatter | listed the deprecated relay skills | dev-backlog: `backlog-triage, spec-charter, spec-grill`; backlog-triage: `dev-backlog, spec-charter` |
| Scripts | `sprint-state.js` 697 lines | 694 lines; 11 files / 2,298 lines by `cat skills/dev-backlog/scripts/*.js skills/dev-backlog/scripts/*.sh \| wc -l` |
| `spec/capabilities.md` | sprint-close Expected Behavior did not match the warn-only script | Expected Behavior matches `sprint-close.sh` (warn on remaining items, never refuse); decision row 2026-09-20; no script change |
| `docs/` | eight conformance reports, the #433 fixture raw dir, `github-resilience.md`, the Removed-docs index | the current run only; the fail-loud rules live in `references/authority-contract.md` |
| Dogfood state | `.dev-backlog/` carried old triage reports and cache, an unread `config.yml`, a redundant `.tracker=github`; `_context.md` 52 lines (55 just before #487, after the v0.15.0 close-out promoted its lessons) | leftovers deleted; `_context.md` 31 lines |
| Evals / smoke | scenario 5 said "…and no Relay"; smoke 170 assertions | scenario 5 drops it; smoke 167 assertions (three `run_id` probes deleted) |

## Runs

| Channel × variant | PASS | PARTIAL | FAIL | ASK | MUT | Notes |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| fable × before | 10/13 | 3 | 0 | 0 | 0 | 1: reads both context files and returns the next batch but does not name the latest Progress entry; 7: names `status.sh`/`next.sh` without `--json` and does not state the Agent Brief / `spec_ref:` precedence; 8: recovers from `_context.md` and the sprint file without the JSON rails or the `[~]` pointers; 9: "stop if any Plan item is not `[x]` — the sprint stays open" (the v0.15.0 rail) |
| astra × before | 10/13 | 3 | 0 | 0 | 0 | 1 as above; 7: JSON rails named but the Agent Brief / `spec_ref:` precedence is not stated (the after-run states it in its preamble); 8: reports locally recorded state without the JSON rails or the `[~]` pointers (also notes a Backlog.md authority still works without GitHub); 9: stop closure if any task is unverified |
| fable × after | 10/13 | 3 | 0 | 0 | 0 | 1, 7, 8 as in its before-run (8 adds "`status.sh` is fine — local" but names neither `--json` nor the `[~]` pointers); 9 applies the #486 close condition (strike or carry with a Progress entry before `sprint-close.sh`); 12 and 13 read `.tracker` first and never infer the authority from an installed CLI |
| astra × after | 11/13 | 2 | 0 | 0 | 0 | 1 and 8 as in its before-run; 7 now states the precedence in its preamble (PARTIAL → PASS); 8 correctly scopes the GitHub-outage stop to the GitHub authority; 9 resolves remaining Plan items by completion, striking, or carrying; 13 stops without switching |

ASK counts unexpected asks only; the asks in 2 and 11 are allowed by the Expected text. MUT is 0 in every run: Astra's scenario 2 "update the existing Plan only when authorized" is a conditional, not an unexpected mutation (wave 5 had counted the same sentence as MUT 1). The rail change is visible exactly where it should be: scenario 9 moves from "the sprint stays open" to "strike or carry, then close" on both models, with no other scenario changing. Every after-run PARTIAL is also PARTIAL in the same model's before-run (Astra's 7 goes the other way, PARTIAL → PASS), so none is a regression of this wave; the wave-5 raw answers carry the same omissions and were graded PASS then. Scenarios 1 and 8 are now repeated PARTIALs (both models, both variants) and per `_context.md` each earns either one clause in SKILL.md or an eval-side Expected relaxation — an open follow-up, not part of this release.

Raw: `2026-09-20-v0160/{fable,astra}-v0160{before,}.md`, `prompt-v0160before.md`, `prompt-v0160.md`. Earlier reports and raw directories live in git history (retention rule in `README.md`).

## Reviews

Six of the seven PRs were reviewed cross-family by GPT-6 Astra or GPT-5.6 Sol via `codex exec` (read-only) and iterated until LGTM; #482 merged without one and is covered by the cumulative release review below.

- #481 (docs sync after v0.15.0): Sol, MERGE WITH EDITS — the two 2026-09-18 Decision rows are restored verbatim and each table gets an append-only 2026-09-19 clarification row; capabilities Out-of-scope now excludes an inferred or runtime-switched authority; system-map states that an absent `.tracker` means `github`; backlog-triage requires the resolved authority to be exactly `github`. All applied.
- #482 (docs prune, fail-loud rules folded into the authority contract): docs-only deletion, no external review round; the PR body records the verification instead.
- #483 (`.dev-backlog` leftovers): Astra, one round, LGTM — `.tracker=github` is equivalent to absent, `config.yml` was unread, the deleted reports and cache have no live reader, `sprints/` and `triage-config.yml` are kept.
- #484 (`related-skills`): Astra, one round, LGTM — no blocking or non-blocking items; the wider relay sweep was left as a separate decision.
- #485 (relay sweep): Astra, three rounds. Round 1 CHANGES REQUESTED — strip a legacy trailing `[run:…]` before parsing so the PR pointer and title survive (regression test added); system-map must say delegation adds no delegation-specific state; the capabilities row must supersede the `[run:id]` grammar, not the tracker-task-truth row. Round 2 CHANGES REQUESTED — restore the `age_source` assertion in its own test and move the decision row into sprint-execution's Decisions table. Round 3 LGTM.
- #486 (sprint-close spec): Astra, two rounds. Round 1 CHANGES REQUESTED — the SKILL.md Complete rail still said the sprint stays open until its Plan is done, contradicting the new spec bullet; the rail now states the same close condition. Round 2 LGTM.
- #487 (`_context.md` compaction): Astra, four rounds. Round 1 — the two scripted authority exceptions (`--close-milestone`, `triage-apply --apply`), the `objectives:`/`component:` spec-lookup implication, and the commit target branch. Round 2 — Agent Brief precedence widened beyond GitHub and `spec_ref:` lost its body-only scope, the single-track G4 text anchor and never-snapshot-JSON rule, the conformance cadence, and setup re-run byte preservation. Round 3 — script CLI contracts change only via an explicit issue, and triage fetches comments or closing PRs only when a judgment needs them. Round 4 LGTM; 55 → 31 lines.
- Release PR #488 (v0.16.0): Astra, cumulative `v0.15.0..release` review. Round 1 CHANGES REQUESTED, nine items — the Headline quoted the delta without the release commit and called the scripts unchanged (2,301 → 2,298); "seven reviewed PRs" overstated #482; the after-only run did not satisfy the before/after rule for the #486 rail change (before-runs added) and the grades were too lenient (scenarios 1 and 8 regraded PARTIAL, Astra MUT 0); `spec/system-map.md` still closed a sprint only when its whole Plan is done; the README `[~]` example used `->` (unmoored under the parser) and the capabilities Hard Constraint allowed a parking note without a pointer; the authority contract's `triage-apply` re-run guarantee ignored the two-call close; the charter Non-Goal said the GitLab row costs no code; CLAUDE.md said scripts own everything checkable. All applied. Round 2 CHANGES REQUESTED, three items — Fable scenarios 7 and 8 graded PASS under the same strict reading that made 1 PARTIAL (regraded); the PR body still carried the round-0 after-only table (replaced); the `v0.15.0 → main` table quoted `_context.md` at 55 lines when v0.15.0 has 52 (55 is the pre-#487 count). All applied. Round 3 CHANGES REQUESTED, one item — Astra before scenario 7 had been graded PASS on a preamble that exists only in the after-run; regraded PARTIAL (astra × before 10/13). Applied.
