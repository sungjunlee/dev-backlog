1. ACTIONS: Read `.dev-backlog/.tracker` (absent = github), `_context.md`, the active sprint file; run `status.sh --json` and `next.sh --json`; name the next actionable batch and the live Issue(s) in it, noting `[~]` items with their PR/branch pointers. MUTATIONS: none. STOP/ASK: no.

2. ACTIONS: Confirm admission (ordered batches/handoff/cross-Issue context). Run `sprint-init.js "topic" --component "slug"` (or `--scope`); it refuses the overlapping track and I do not bypass it. Report the conflict with the two scopes. MUTATIONS: none (refusal creates nothing). STOP/ASK: yes — overlap is a design conflict; ask whether to narrow the scope to a disjoint axis, fold the work into the existing active track's Plan, or wait until that track closes.

3. ACTIONS: Read `_context.md` and both sprint files (`auth`, `billing`); `status.sh --json` for the portfolio, then `next.sh --track auth` and `next.sh --track billing`; name each track's state and next batch. MUTATIONS: none. STOP/ASK: no — disjoint tracks coexist as a portfolio; I ask only if the user hasn't said which track to work.

4. ACTIONS: Ordered multi-Issue batches admit a sprint. `gh issue view N --json body,comments` for each of the three (fail-closed); `setup-dev-backlog.js`; `sprint-init.js "topic"` (`--scope` if a path axis fits); write Goal, Plan as three sequential batches (one Issue each, complete `#N` refs, estimates), empty Running Context/Progress. Spec axis absent: degrade per `references/spec-fallback.md` (no objectives, no charter check); don't install craftkit. MUTATIONS: local only — new `.dev-backlog/`, `_context.md`, active sprint file; tracker unchanged. STOP/ASK: no; mention that spec-* is optional.

5. ACTIONS: Sprint-free default: `gh issue view N --json body,comments` (Agent Brief / `spec_ref:` precedence), implement, verify each AC, open a PR, close the Issue with `gh issue close N` after merge. No `setup-dev-backlog.js`, no sprint. MUTATIONS: tracker — AC check-offs and Issue closure, all explicit; local — code/PR only, no `.dev-backlog/`. STOP/ASK: no.

6. ACTIONS: `gh issue view 42 --json body,comments`; take newest `## Agent Brief` comment over body, `spec_ref:` over both. Implement, verify AC 1–3 individually, check each off on the Issue only after verification; PR; if a sprint is admitted, mark Plan `[~]` with the PR pointer and add a Progress note. MUTATIONS: tracker — AC checkboxes (and close after merge); local — sprint Plan/Progress only if admitted. STOP/ASK: no, unless the read fails — then diagnose and stop.

7. ACTIONS: Read `.tracker`, `_context.md`, active sprint if any; `status.sh`/`next.sh`; if no sprint, `gh issue list` and read the candidate Issue live. No local task files is normal — the Issue is the spec. MUTATIONS: none until work starts. STOP/ASK: no; ask only which Issue if the user gave no target and none is on a Plan.

8. ACTIONS: Read `_context.md` and the sprint file for orientation only (`status.sh` is fine — local). Any `gh issue view` fails → fail-closed: report state from local files as "last recorded", not as truth. MUTATIONS: none — no AC changes, no closures, no Plan `[~]`/`[x]`, no sprint edits. STOP/ASK: yes — authority read failed; ask the user to restore GitHub access (auth/network) before executing or mutating anything.

9. ACTIONS: Per task: re-read each live Issue, verify AC, close via `gh issue close N`, mark `[x]`; strike/carry unfinished items with a Progress entry. Run `sprint-close.sh` (`--track` if portfolio), read doctor verdicts; then promote the project-level Running Context lines to `_context.md`; sprint file becomes immutable record. MUTATIONS: tracker — closures; local — `status: completed`, final Progress, `_context.md` additions. STOP/ASK: no, unless doctor reports failures or an AC is unverified — then stop before closing.

10. ACTIONS: Re-read `gh issue view N --json body,comments` (newest Agent Brief / `spec_ref:` win); diff against what I built; verify against the current spec, redo divergent parts; check only AC that hold now. MUTATIONS: tracker — AC per the new spec; local — Running Context note recording the change and its impact, Progress entry. STOP/ASK: ask if the change invalidates completed work or the batch plan (scope grew or contradicts merged work); otherwise no.

11. ACTIONS: Do not read `backlog/sprints/` as live state. Run `setup-dev-backlog.js`, which creates `.dev-backlog/` and migrates sprints/config/triage, leaving `backlog/tasks/`, `docs/`, `completed/`; then orient normally via `_context.md` and the migrated active sprint, `status.sh`/`next.sh`. MUTATIONS: local only — new `.dev-backlog/` with migrated sprint and config; tracker untouched. STOP/ASK: brief confirmation before migrating since it moves files; orientation itself needs none.

12. ACTIONS: Tracker says `backlog`, so no GitHub needed and none inferred from installed CLIs. `backlog task 7 --plain` is the spec (`spec_ref:` overrides). Implement, verify AC, commit/branch; close with `backlog task edit 7 -s Done` (or the repo's configured terminal status). No Agent Brief precedence, no `backlog-triage`, no `--close-milestone`. MUTATIONS: tracker — task 7 AC/status; local — sprint Plan/Progress if admitted. STOP/ASK: no.

13. ACTIONS: Attempt `backlog task N --plain`; it fails (command not found) → authority read failed, fail-closed. Diagnose: report that the declared authority is `backlog` and the CLI is missing. Do not switch to `gh` even if installed, do not read task text from the sprint file. MUTATIONS: none. STOP/ASK: yes — ask the user to install the Backlog.md CLI or explicitly change `.dev-backlog/.tracker`; nothing executes until a live read succeeds.
