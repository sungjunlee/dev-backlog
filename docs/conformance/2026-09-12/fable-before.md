1. ACTIONS: Read `backlog/sprints/_context.md`, then the single active sprint file (Goal, Plan, Running Context, latest Progress entry); run `status.sh --json` and `next.sh --json` to confirm the first unchecked/`[~]` batch. Name the latest Progress entry and the next actionable batch. MUTATIONS: none. STOP/ASK: no.

2. ACTIONS: Confirm a Sprint Admission trigger; run `sprint-init.js "<topic>" --dry-run` with the proposed `--component`/`--scope`; `scopesOverlap` reports collision with the existing `status: active` track. Refuse, naming the conflicting track. MUTATIONS: none (no sprint file, no milestone). STOP/ASK: yes — ask the user to either declare a disjoint `component:`/`scope:` (then a second track is admitted) or complete/close the conflicting track first via `sprint-close.sh --track <slug>`.

3. ACTIONS: Read `_context.md`; run `status.sh --json` / `next.sh --json` for the portfolio view (one stanza per track: `auth` → `src/auth/**`, `billing` → `src/billing/**`) and read each sprint's Goal/Plan/Running Context/Progress; run `backlog-doctor.js` (passes, scopes disjoint); run `next.sh --track auth` and `next.sh --track billing` to name each next batch. MUTATIONS: none. STOP/ASK: no; ask only which track to work if the user then says "continue".

4. ACTIONS: `scripts/setup-dev-backlog.js --tracker github --non-interactive` to bootstrap `backlog/` (`.tracker` = github); `gh issue list` to inspect open Issues; confirm a Sprint Admission trigger (ordered multi-Issue batches etc.); `sprint-init.js "<topic>" --dry-run` then real run with no `--component`, `objectives:` and `component:` omitted entirely (no `spec/`, no `CHARTER.md`, no `spec-*` skill — never require `../spec-charter/...`); write Goal, ordered parallel-safe batches, estimates. MUTATIONS: local `backlog/.tracker`, `backlog/sprints/YYYY-MM-<topic>.md`; GitHub milestone only if the adapter reports `milestones`. STOP/ASK: ask which Issues go in the sprint if selection is ambiguous; otherwise no.

5. ACTIONS: Sprint-free path. `effective-task-spec.js #N` to resolve spec/AC/lifecycle from the live Issue; implement on a branch; verify each AC; open PR (`Fixes #N` only if closing-semantics intentionally used); update Issue AC/lifecycle. MUTATIONS: code branch + PR; Issue AC checkboxes/state. No `backlog/`, no sprint, no Projects board, no generated memory, no optional skill. STOP/ASK: no (bootstrap `backlog/` only if the user wants it later).

6. ACTIONS: `effective-task-spec.js 42` → read `effective_spec`, three AC items, lifecycle, `source_ref`, digest; check `status.sh --json` for an admitted sprint containing #42 and its Running Context; mark Issue in-progress if useful; implement; verify each of the three AC individually; re-run resolver to confirm digest unchanged before checking off. MUTATIONS: GitHub Issue AC checkboxes and lifecycle; PR; Plan `[~]`→`[x]`, Running Context, Progress only if #42 is in an active sprint — otherwise no local files. STOP/ASK: stop clearly if the resolver fails (legacy mirror is diagnostic only).

7. ACTIONS: Read `_context.md`; `status.sh --json` and `next.sh --json` to recover sprint continuity and in-flight `[~]` items; for the next task run `effective-task-spec.js TASK_REF` — an explicit `spec_ref` wins, else the live Issue body; proceed to Work. MUTATIONS: none during recovery; never `sync-pull.js --legacy-export`, never create task files. STOP/ASK: no.

8. ACTIONS: Read `_context.md` and active sprint(s); `status.sh --json` / `next.sh --json` to recover execution continuity and every `[~]` owner/pointer (PR, run dir, dispatcher). `effective-task-spec.js` cannot resolve without GitHub. MUTATIONS: none — no Plan checkbox, AC, or lifecycle changes; no legacy-export read as fallback. STOP/ASK: yes — stop before any execution or AC/lifecycle claim; report recovered state and ask the user to restore GitHub access or provide an explicit `spec_ref`.

9. ACTIONS: Verify every Plan item `[x]` and Issues closed; run `sprint-close.sh` (runs `backlog-doctor.js`, prints `reassess_signal`); promote project-level Running Context entries to `_context.md`; write final Progress entry; leave the sprint file in place. MUTATIONS: sprint file `status: completed` + Progress; `_context.md` appended. No `backlog/tasks/`, no `backlog/completed/`. GitHub: none (milestone close only if capability gated and intended). STOP/ASK: surface any reassess recommendation to the user; never `amend spec/*` unattended.

10. ACTIONS: Re-run `effective-task-spec.js TASK_REF`; compare the new digest/source revision to the one I started from; review the changed spec/AC and re-scope the in-progress implementation; note the change in the sprint's Running Context/Progress if admitted. MUTATIONS: no local task file written; no background sync; `sync-pull.js --legacy-export` only on an explicit rollback/diagnostic request. STOP/ASK: yes if the change alters scope or AC materially — confirm with the user before continuing or checking off any AC.
