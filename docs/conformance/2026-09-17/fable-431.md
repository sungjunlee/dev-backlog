1. ACTIONS: Read `.dev-backlog/sprints/_context.md` and the active sprint file; run `status.sh --json` and `next.sh --json`; report the sprint's Goal, which Plan items are `[x]`/`[~]`/`[ ]`, and the next actionable batch with its live Issue refs. MUTATIONS: none. STOP/ASK: no — orient is read-only.

2. ACTIONS: Read `_context.md` and the existing active track's frontmatter (`component:`/`scope:`); run `sprint-init.js "topic" --scope "<globs>"` (or `--component`), which refuses the overlapping track and fails loud. Do not hand-write a second sprint file. MUTATIONS: none (init refused). STOP/ASK: yes — report the overlap and ask whether to narrow the scope to a disjoint axis, fold the work into the existing active track's Plan as a later batch, or wait for that track to close.

3. ACTIONS: Read `_context.md` and both active sprint files; run `status.sh --json` (portfolio view), then `next.sh --track auth` and `next.sh --track billing` for each track's next batch; confirm the two `scope:` declarations are disjoint. MUTATIONS: none. STOP/ASK: no; ask only which track the user wants to work if the next request is ambiguous.

4. ACTIONS: Ordered multi-Issue batches admit a sprint. Run `setup-dev-backlog.js --tracker github --non-interactive` to bootstrap `.dev-backlog/`; run `sprint-init.js "topic"` without `--component`/`--objectives` (no spec axis, so those fields degrade per `references/spec-fallback.md`); resolve each of the three issues with `effective-task-spec.js #N` to confirm AC exist; write Goal and three ordered single-item batches (dependents in later batches). MUTATIONS: local only — new `.dev-backlog/` config, `_context.md`, and `.dev-backlog/sprints/YYYY-MM-<topic>.md` with `status: active`; no tracker writes. STOP/ASK: no; note in the response that `spec/` is absent and `objectives:` is omitted rather than inferred.

5. ACTIONS: Sprint admission fails (single Issue, no batches, no handoff), so stay sprint-free. Run `effective-task-spec.js #N` (fail-closed if `gh` is unavailable), implement on a branch, verify each AC, open a PR, then close the Issue via the adapter's close verb after merge. I would not create `.dev-backlog/` just for this. MUTATIONS: tracker — AC checkboxes ticked and Issue closed after verification; local — code/PR only, no sprint file. STOP/ASK: no, except a pause before the merge/close if the user hasn't said I may merge.

6. ACTIONS: `work` mode. Run `effective-task-spec.js #42` for the effective spec, three AC, lifecycle, `source_ref`, digest; implement (or delegate via dev-relay); verify each AC item against the actual result; tick only verified checkboxes on the live Issue; open PR. If a sprint is active and #42 is on its Plan, mark `[~]` with the PR pointer and add a Progress note. MUTATIONS: tracker — AC checkboxes on #42 (explicit edit), PR link; local — sprint Plan/Progress only if admitted. STOP/ASK: no; stop if the resolver fails (diagnose, don't execute).

7. ACTIONS: Read `_context.md` and any active sprint file; run `status.sh --json` / `next.sh --json`; no local task files is the normal state — task truth is the live tracker, so resolve the named next Issue with `effective-task-spec.js`. MUTATIONS: none. STOP/ASK: no.

8. ACTIONS: Read `_context.md` and the active sprint file for the readable picture; run `status.sh`, which will report the tracker adapter as unavailable. Report the last-known Plan/Progress state clearly labeled as stale-possible. MUTATIONS: none. STOP/ASK: yes — the tracker is fail-closed: I will not execute tasks, tick AC, change lifecycle, or fall back to local files as authority until `gh` access is repaired; I ask the user to restore access (or confirm read-only planning discussion only).

9. ACTIONS: Per task, re-resolve each Plan issue with `effective-task-spec.js`, verify AC, ensure each is merged and closed via the adapter; then `sprint-close.sh` (runs `backlog-doctor.js`, flips `status: completed`, appends final Progress, prints reassess signal); after it succeeds, copy the project-level Running Context entries into `.dev-backlog/sprints/_context.md`. MUTATIONS: tracker — any remaining Issue closures (explicit); local — sprint file `status: completed` (never flipped back), `_context.md` gains promoted context. STOP/ASK: yes if any Plan item is not `[x]` or an Issue is still open — `sprint-close.sh`/doctor will flag it; I ask whether to finish, defer to a new Issue, or drop the item rather than close with stale work.

10. ACTIONS: Re-run `effective-task-spec.js #N`; compare the new digest/AC against what I was implementing; adjust implementation to the current effective spec and verify against the new AC only. If the change conflicts with work already done or the sprint's Goal, add a Running Context/Progress note. MUTATIONS: tracker — none automatically (the live Issue is truth; I never revert it); local — Progress/Running Context note if a sprint is admitted. STOP/ASK: yes if the change invalidates completed work or scope materially — report the diff and confirm direction before continuing.

11. ACTIONS: `.tracker` = `files` means the Backlog.md CLI is the sole authority; never switch adapters. Run `effective-task-spec.js BACK-7` (via the `backlog` adapter, fail-closed if the CLI fails); implement on a branch; verify both AC; tick the two AC via the Backlog.md CLI; open PR; close `BACK-7` with the adapter's close verb after merge. No sprint (self-contained task). MUTATIONS: tracker — BACK-7 AC checked and status closed through `backlog` CLI; local — code/PR only. STOP/ASK: no; stop and repair if the `backlog` CLI is unavailable.

12. ACTIONS: Orient reads `.dev-backlog/sprints/_context.md`, which does not exist; `status.sh` will report no `.dev-backlog/`. The Plan rail says `setup-dev-backlog.js --tracker github --non-interactive` migrates a legacy `backlog/` layout (sprints, `.tracker`, config, triage) and leaves `backlog/tasks/`, `docs/`, `completed/` in place. Meanwhile I can read `backlog/sprints/<active>.md` for the picture. MUTATIONS: migration creates `.dev-backlog/` and moves sprint files/`.tracker`/config — a local, one-time layout change; no tracker writes. STOP/ASK: yes — orient is meant to be read-only, and migration is a structural change; I report the legacy layout, describe what the migration moves and leaves, and ask for approval before running setup.
