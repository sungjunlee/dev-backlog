1. ACTIONS: Read `backlog/sprints/_context.md` and the active sprint file; run `status.sh --json` and `next.sh --json`; report the Plan state (which items are `[ ]`/`[~]`/`[x]`) and name the next actionable batch and the next live Issue. MUTATIONS: none. STOP/ASK: no; orient is read-only.

2. ACTIONS: Read `_context.md` and the existing active sprint to see its `component:`/`scope:`; run `sprint-init.js "topic" --scope ...` (or `--component`), which refuses the overlapping track. Report the overlap. MUTATIONS: none (init refuses; no sprint file written). STOP/ASK: yes; the user must choose: narrow the new scope to be disjoint, fold the work into the existing active track's Plan, or close the existing sprint first. I do not force or bypass the overlap check.

3. ACTIONS: Read `_context.md` and both sprint files; run `status.sh --json` for the portfolio view, then `next.sh --track auth` and `next.sh --track billing`. Report each track's Plan state and next batch. MUTATIONS: none. STOP/ASK: no, except to ask which track to work on if the user's intent is ambiguous.

4. ACTIONS: Run `setup-dev-backlog.js --tracker github --non-interactive` to create `backlog/`; check Sprint Admission (multi-Issue ordered batches, handoff, cross-Issue context) against the open issues via `gh issue list`; if admitted, `sprint-init.js "topic" [--milestone]` without `--component`/`--objectives` (spec axis absent, degrade per `references/spec-fallback.md`), then write Goal, ordered Plan batches with `#N` refs, estimates. MUTATIONS: local only: `backlog/` config, `_context.md`, new sprint file; no GitHub changes. STOP/ASK: ask if admission is unclear, and confirm scope/batch grouping before writing; do not install craftkit or invent spec files.

5. ACTIONS: No sprint (fails Sprint Admission: one self-contained Issue). Run `effective-task-spec.js #N` to resolve AC; implement directly on a branch, verify each AC, open a PR, tick AC via explicit `gh issue edit`/comment, close the Issue after merge. MUTATIONS: GitHub: AC checkboxes and Issue closure (explicit); local: code/branch only, no `.dev-backlog/` created. STOP/ASK: no, unless AC are ambiguous or resolution fails.

6. ACTIONS: Run `effective-task-spec.js 42` for AC, lifecycle, `source_ref`, digest; implement; verify each of the three AC items; if a sprint is admitted, set Plan item `[~]` with branch/PR pointer; after verification check off AC in the Issue explicitly and open PR. MUTATIONS: GitHub: AC checkboxes (only after verification), PR; local: code, sprint Plan/Progress if one exists. STOP/ASK: stop if `effective-task-spec.js` fails (diagnose, do not execute or touch AC/lifecycle); otherwise no.

7. ACTIONS: Read `_context.md` and any active sprint file; run `status.sh --json` and `next.sh --json`; resolve the next Issue with `effective-task-spec.js` live from GitHub. No local task files are needed; the tracker is authority. MUTATIONS: none until work starts. STOP/ASK: no.

8. ACTIONS: Read `_context.md`, the active sprint file (Plan, Running Context, Progress) to reconstruct state; attempt `status.sh`/`effective-task-spec.js`, which will fail without GitHub. Report what the local files say and what cannot be verified. MUTATIONS: none. STOP/ASK: yes; per the Work boundary, without live resolution I do not execute tasks or change AC/lifecycle, and the tracker is fail-closed (no fallback to local files as authority). Ask the user to restore GitHub access or explicitly confirm read-only planning.

9. ACTIONS: Per task, re-run `effective-task-spec.js`, confirm every AC verified and Issue closed; then `sprint-close.sh` (runs `backlog-doctor.js`, flips `status: completed`, appends final Progress, prints reassess signal); then promote project-level Running Context into `_context.md`. MUTATIONS: local: sprint `status: completed`, Progress entry, `_context.md` additions; GitHub: only closing any remaining verified Issues explicitly. STOP/ASK: ask if the doctor reports unfinished Plan items or unverified AC; never flip completed back.

10. ACTIONS: Re-run `effective-task-spec.js #N`; compare the new digest/AC to what I was working from; reconcile my implementation against the new effective spec; update sprint Running Context with the change. MUTATIONS: local: Running Context note; GitHub: none automatic. STOP/ASK: yes, if the change alters scope or invalidates completed work; surface the diff and ask whether to proceed under the new AC. Small clarifications proceed.

11. ACTIONS: This SKILL.md says `.tracker` is github-only (`setup-dev-backlog.js --tracker github`); a `files`/Backlog.md tracker is outside this contract. Attempt `effective-task-spec.js BACK-7` and see whether the resolver supports it. If it resolves, work sprint-free: implement, verify both AC, tick via the tracker's explicit write, mark done. MUTATIONS: tracker AC/status only after verification; no sprint. STOP/ASK: yes, if resolution fails or the adapter is unsupported by this skill version; do not fall back to editing Backlog.md files directly as authority.

12. ACTIONS: This SKILL.md's contract uses `backlog/` as the hub, so the repo matches its layout: read `backlog/sprints/_context.md` and the active sprint, run `status.sh --json` and `next.sh --json`, report state and next Issue. Note that `backlog/.tracker` is `github` and `config.yml` is present. MUTATIONS: none. STOP/ASK: no for orient; if the user wants migration to a `.dev-backlog/` layout, ask before moving files since that is not covered by this contract.
