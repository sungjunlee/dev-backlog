1. **ACTIONS:** Read `.dev-backlog/sprints/_context.md`, then the active sprint file; run `status.sh --json` and `next.sh --json`; report current Plan state (`[x]`/`[~]`/`[ ]` counts), any `[~]` items with their PR/branch pointers, and the next actionable batch. **MUTATIONS:** none. **STOP/ASK:** no; orient is read-only. I would flag any `[~]` with no pointer as a possible repair case (`references/checkbox-repair.md`) rather than fixing it silently.

2. **ACTIONS:** Read `_context.md` and the existing active sprint to learn its `component:`/`scope:`; run `sprint-init.js "topic" --scope/--component ...`, which refuses an overlapping track and fails loud. Do not hand-craft a sprint file to bypass the refusal. **MUTATIONS:** none (init refuses). **STOP/ASK:** yes; report the overlap and ask whether to narrow the scope to a disjoint axis, add the issues to the existing active track's Plan, or wait until that track closes. Overlap is exactly what the rail guards, so I don't choose for the user.

3. **ACTIONS:** Read `_context.md`, then both active sprint files; run `status.sh --json` (portfolio view) and `next.sh --json`, then `--track auth` and `--track billing` for per-track detail. Report each track's Goal, Plan state, and next batch, and confirm the scopes are disjoint. **MUTATIONS:** none. **STOP/ASK:** no; if the user then says "work", I ask which track unless the issue number makes it obvious.

4. **ACTIONS:** Confirm no craftkit skills are installed (`spec-fallback.md` governs degradation: `objectives:`/`component:` simply stay unset). Run `setup-dev-backlog.js --tracker github --non-interactive` to create `.dev-backlog/`. Apply Sprint Admission: list open issues with `gh issue list`, and only if they need ordered batches / cross-issue context run `sprint-init.js "topic"` (optionally `--scope`), then write Goal, batched Plan with `#N` refs, estimates. **MUTATIONS:** local `.dev-backlog/` created and one sprint file; tracker: none. **STOP/ASK:** yes, briefly, before `sprint-init.js` if the open issues don't clearly justify a sprint (default path is sprint-free); also would not fabricate `spec/*` files.

5. **ACTIONS:** Sprint Admission says no sprint: single self-contained issue, no handoff. Run `effective-task-spec.js #N` to get AC and lifecycle; implement on a branch; verify each AC; open a PR; close the issue via the adapter close verb after merge. `.dev-backlog/` is not required for this path. **MUTATIONS:** tracker: AC checkboxes ticked and issue closed after verification; local: code/PR only, no `.dev-backlog/`. **STOP/ASK:** no, unless resolution fails (fail-closed) or the AC is ambiguous.

6. **ACTIONS:** Run `effective-task-spec.js #42`; note `source_ref` and digest (resolver picks `spec_ref` > `## Agent Brief` comment > body). Implement directly (or via dev-relay if it fits), verifying each of the three AC items in turn and checking each off in the Issue only after verification. If a sprint had admitted #42, mark the Plan item `[~]` with the branch/PR pointer; here there is none. **MUTATIONS:** tracker: three AC checkboxes, later close; local: code branch/PR. **STOP/ASK:** no; stop only if `effective-task-spec.js` fails, in which case diagnose and do not execute or touch AC.

7. **ACTIONS:** Read `_context.md` and any active sprint file if `.dev-backlog/` exists; run `status.sh --json` / `next.sh --json`. With no active sprint, name the next live Issue from the tracker (`gh issue list` / `next.sh`). Resolve whatever I work with `effective-task-spec.js`. **MUTATIONS:** none for orientation. **STOP/ASK:** no; I do not create task files locally, since the tracker is the sole task authority.

8. **ACTIONS:** Read `_context.md` and the active sprint file for the readable picture; `status.sh` gives tracker-neutral sprint state; `next.sh` can name the batch. Attempt `effective-task-spec.js` for the next task; it will fail-closed without GitHub. **MUTATIONS:** none. **STOP/ASK:** yes; the adapter is unavailable, so I report what the sprint file says, diagnose the `gh` auth/network problem, and do not implement, check off AC, or fall back to local files as a substitute authority.

9. **ACTIONS:** For each Plan task, re-resolve with `effective-task-spec.js`, verify AC, ensure merged and closed via adapter close, mark `[x]`. Then run `sprint-close.sh` (it runs `backlog-doctor.js`, flips `status: completed`, appends final Progress, prints reassess). After success, promote project-level Running Context into `_context.md`; leave the sprint file as record. **MUTATIONS:** tracker: closes for any still-open verified tasks; local: sprint `status: completed`, Progress entry, `_context.md` append. **STOP/ASK:** yes if doctor warns (e.g., an unmoored `[~]`) or any Plan item is unverified; also never flip completed back.

10. **ACTIONS:** Re-run `effective-task-spec.js` and compare the digest with the one I started from; read the diff in AC/body. Re-verify work against the new effective spec; adjust implementation if AC changed; add a Running Context/Progress note in the sprint (if admitted) explaining the change. **MUTATIONS:** local sprint notes; tracker: only AC checkboxes I can re-verify, no rewriting of the Issue. **STOP/ASK:** yes if the change invalidates completed work or scope; I don't silently expand or reinterpret.

11. **ACTIONS:** `.tracker` = `files`, so use the Backlog.md CLI per `adapter-ports.md`; never switch to `gh`. Run `effective-task-spec.js BACK-7` to get AC; no sprint per admission. Implement, verify both AC items, check them off through the adapter, then close with the adapter's close verb. **MUTATIONS:** tracker (Backlog.md files) AC checkboxes and task closure; local: code branch/PR; no sprint file. **STOP/ASK:** no, unless the `backlog` CLI is unavailable, which is fail-closed.

12. **ACTIONS:** The contract only knows `.dev-backlog/`; `backlog/` is the legacy layout. Read `backlog/sprints/*` and `backlog/.tracker` for orientation only, report what I see, and note the layout mismatch: scripts (`status.sh`, `next.sh`, `sprint-init.js`) target `.dev-backlog/` and will not find state. **MUTATIONS:** none. **STOP/ASK:** yes; propose migrating (move `backlog/` to `.dev-backlog/` or run `setup-dev-backlog.js --tracker github --non-interactive` then move the sprint file) and ask before moving files, since it changes the shared execution record.
