# Conformance: 2026-09 task authority (epic #472, charter rev 20)

Before/after pair. The wave changed a SKILL.md rail for the first time since v0.12.0 — the four `gh` rails became one `## Task Authority` table (157 → 165 lines) — and added two eval scenarios (12, 13) for the GitHub-less path. **Before** = the new thirteen-scenario eval file × the v0.14.0 SKILL.md (`prompt-w5before.md`); **after** = the same eval file × the merged SKILL.md (`prompt-w5after.md`). Harness as in `2026-09-17-skill-lightening.md`: Expected stripped, rubric PASS/PARTIAL/FAIL with ASK/MUT flags, graded by the session against `tests/evals/dev-backlog.md`.

## What changed under the models (v0.14.0 → main)

| Surface | v0.14.0 | main |
| --- | --- | --- |
| Task authority | GitHub Issues, hardcoded in four `gh` rails | `.dev-backlog/.tracker` one line (absent = `github`; `backlog`, `files` alias; `gitlab`) + a read / create / close table |
| `SKILL.md` | 157 lines | 165 lines; no new reference file |
| Scripts | `tracker: "github"` hardcoded in JSON; `--close-milestone` unguarded | `lib.js readTaskAuthority`; JSON `tracker` field reflects the line (byte-identical when absent); `--close-milestone` refuses non-GitHub; setup names the authority; +28 shipped lines |
| Spec | charter rev 19 "tracker generality" Non-Goal | rev 20 "tracker abstraction in code" Non-Goal; GitLab row is the stated unmeasured exception |

## Runs

| Channel × variant | PASS | PARTIAL | FAIL | ASK | MUT | Notes |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| fable × before | 11/13 | 0 | 2 | 0 | 0 | 12: treats `.tracker` as ignorable and demands a GitHub remote; 13: proceeds with `gh` and suggests deleting `.tracker`. 1–11 as in wave 4 |
| astra × before | 11/13 | 0 | 2 | 0 | 1 | 12: "ignore `.tracker` as an authority selector", asks for a GitHub repo; 13: proceeds with `gh`. 1–11 as in wave 4 (MUT: Plan fold in 2) |
| fable × after | 13/13 | 0 | 0 | 0 | 0 | 12: `backlog task 7 --plain` → verify AC → `backlog task edit 7 -s Done` (or the configured terminal status), "do not use `gh` even if installed"; 13: fail-closed, stop, never switch authority |
| astra × after | 13/13 | 0 | 0 | 0 | 1 | 12 and 13 as above; 8 now says "if the declared authority is Backlog.md, proceed through it" (correct: the GitHub-outage stop applies to the GitHub authority); MUT: Plan fold in 2 |

The two new scenarios flip FAIL → PASS on both models with no regression on 1–11. Every after-run read `.tracker` first (Astra names it in 1, 6, 7, 8, 12, 13) and no after-run inferred the authority from an installed CLI.

Raw: `2026-09-18-w5/{fable,astra}-w5{before,after}.md`, `prompt-w5before.md`, `prompt-w5after.md`. The wave-4 raw directory is pruned in this commit per the retention rule; `2026-09-18-wave4.md` stays as the record.

## Reviews

- #478 (session, #475): Sol via codex, MERGE WITH EDITS — GitLab row contradicted the measured-consumer rule (now a stated exception); charter "Wrapping `gh`" and the CLAUDE.md arrow still fixed the task path to `gh`; `Done` is Backlog.md's default terminal status, not a constant; the prose test had stopped asserting ownership. All applied. Backlog.md and `glab` verbs confirmed against their docs by the reviewer.
- #479 (Sonnet subagent, #476): Astra via codex; verdict recorded on the PR. Acceptance amended ≤ 15 → ≤ 30 shipped lines: the 15-line draft was three 150-column one-liners.
- Release PR: Astra via codex, cumulative v0.14.0..main; verdict recorded on #477.
