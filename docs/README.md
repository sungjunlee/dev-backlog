# Project Docs

`docs/` holds only the current conformance run ([`conformance/`](conformance/)).
Durable contracts live in `spec/*` and `skills/*/SKILL.md` + `references/`;
everything historical (design records, pilots, earlier conformance runs,
removed docs) lives in git history and release tags, not here. Do not re-read
history before changing current behavior.
