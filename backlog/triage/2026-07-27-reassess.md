# Reassess Report — 2026-07-27

Report-only stale-spec review triggered by the sprint-close reassess signal (doctor clean; 3 sprints closed since the last reassess `2026-07-20`, threshold 3). The three closes are the v0.9.0 local-substrate release (`2026-07-v0-9-0-local-substrate`, O8/O9) and the two tracks of the adoption-gate portfolio (`2026-07-prose-guard` O9, `2026-07-track-entry` O1/O3). This dated report resets the counter. Report-only: no spec files were edited **by this report**. The charter change described below was applied earlier today by a human-confirmed `#333` amend, not by this pass.

## Evidence

- `backlog-doctor`: 13/13 PASS across the two-track portfolio before each close; `active_sprint` reported "2 active tracks, scopes disjoint" — the first time this repo has run a real portfolio through close.
- `objectives-check --json`: `charterObjectiveIds` = O1, O3, O4, O5, O6, O7, O8, O9; `drift` empty. The `[implemented]` token parses only because #335 landed first; the pre-#335 parser run against the same file returns `["O1","O3","O4","O5","O6","O7"]` and exits 1.
- `capabilities-doctor`: 5 capabilities, 200 lines, within budget.
- Charter is at revision 10, last amended 2026-07-27 (#333, human-confirmed diff).
- Adoption measured across the 17 repos consuming dev-backlog besides this one: 146 sprints (O1); `backlog/triage/` reports in 7 repos (O4); dated reassess reports in 3 — consumer-A, beopsuny-skill, aibris (O5); 10 repos running sprints with no `spec/` axis at all, up to 15 sprints each (O7); **0** repos using `local` (O8); **0** repos setting a non-default tracker (O9).

## No Change

- **Charter Objectives** — re-evaluated in full today under the new rule. O8 and O9 moved to `[implemented]`; O1, O4, O5, O7 stayed `[validated]` with adoption evidence now cited inline. O3 stays `[active]`, O6 stays `[deferred]`. Nothing further is owed here this cycle; this report exists to record that the re-evaluation happened and to reset the counter, not to redo it.
- **`spec/capabilities.md`** — `sprint-execution` gained no new durable behavior from `--component`: the flag is an entry path to a contract the capability already states (`scopesOverlap`, one axis per track). `tracker-task-truth` is unchanged by the prose guard, which asserts existing claims rather than adding any.
- **`spec/system-map.md`** — the v0.8.0 stale-evidence line flagged on 2026-07-20 was cleared in v0.9.0. No new drift found; the storage/flow description already matches the JSON-canonical local store.

## System Map Candidates

- None this cycle.

## Grill Candidates

- **`sprint-execution` — the scopeless-warning rule** — evidence: `backlog-doctor.js:322` warns only at `scopeless.length >= 2`, and `sprint-init.js:229` mirrors it, so a repo with one scopeless active track and one `component:` track sits in an unprovable state with no signal. `spec/capabilities.md` `sprint-execution` Expected Behaviors states the pair rule, so changing it is an Expected-Behavior amendment, not an implementation fix. Filed as **#337** with both candidate rules and the evidence for each. next: human-gated `spec-grill sprint-execution` when #337 is decided.

## Missing Evidence

- **O4's external evidence is weaker than its citation implies.** `backlog/triage/` reports exist in 7 other repos, which proves the triage command was *run* there, not that drift was *caught* without manual triage. The predicate asks for the latter. The citation is honest about what it counts, but a future pass should either find a caught-drift instance outside this repo or narrow the predicate.
- **O9 is `[implemented]` for a reason worth restating.** Zero repos set a non-default tracker, so the multi-adapter ownership guarantee has exactly one adapter in production everywhere. The invariant is exercised constantly; the thing that makes it an *objective* — that a second adapter could own truth — is not.

## Verdict

Axis is current. One open contract question (#337) routed to a human-gated pass; no unattended spec edits made or warranted.
